import React from 'react';
import { Container } from 'react-bootstrap';

const MyBookings = () => {
    return (
        <Container>
            <h1>This is my bookings</h1>
        </Container>
    );
};

export default MyBookings;